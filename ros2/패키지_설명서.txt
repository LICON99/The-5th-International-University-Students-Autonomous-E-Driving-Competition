### 1. 주요 패키지 및 파일 설명

이 저장소는 벨로다인 라이다 센서를 ROS 2에서 사용하기 위한 드라이버 및 유틸리티 모음입니다.

*   **`velodyne_driver`**: 하드웨어와 직접 통신하는 핵심 패키지입니다. 네트워크를 통해 라이다의 원시 데이터(Raw data)를 수신하여 `velodyne_msgs/VelodyneScan` 메시지로 발행합니다.
*   **`velodyne_pointcloud`**: 드라이버가 발행한 원시 데이터를 3차원 점군 데이터(`sensor_msgs/PointCloud2`)로 변환합니다. 보정(Calibration) 파일(.yaml)을 사용하여 정확한 3D 좌표를 계산합니다.
*   **`velodyne_laserscan`**: 3D 점군 데이터를 2D 레이저 스캔(`sensor_msgs/LaserScan`) 데이터로 변환해 줍니다. 2D SLAM이나 장애물 회피에 유용합니다.
*   **`velodyne_msgs`**: 이 패키지에서 사용하는 전용 메시지 타입들이 정의되어 있습니다.
*   **`velodyne`**: 위 패키지들을 묶어주는 메타 패키지입니다.

---

### 2. 라이다 실행 방법

이미 `build`와 `install` 폴더가 있는 것으로 보아 빌드가 완료된 상태로 보입니다. 라이다를 실행하기 위해서는 먼저 환경 설정을 로드해야 합니다.

#### 1) 터미널 환경 설정
```bash
source install/setup.bash
```

#### 2) 라이다 실행 (드라이버 및 점군 변환)
보통 `velodyne_pointcloud` 패키지에 있는 런칭 파일을 사용하면 드라이버와 변환 노드를 한 번에 실행할 수 있습니다.

**VLP-16 (Puck) 모델 기준:**
```bash
ros2 launch velodyne_pointcloud velodyne_convert_node_cloud_component_launch.py model:=VLP16
```

**전체 센서 구성을 포함한 실행 (추천):**
실제 하드웨어가 연결되어 있다면, 드라이버까지 포함된 전체 런칭 파일을 사용하십시오.
```bash
ros2 launch velodyne_driver velodyne_driver_node_cloud_launch.py model:=VLP16
```
*(참고: `model` 파라미터에는 `VLP16`, `32E`, `64E` 등을 지정할 수 있습니다.)*

#### 3) 데이터 확인
새 터미널에서 다음 명령어를 통해 데이터가 들어오는지 확인할 수 있습니다.
```bash
# 점군 데이터 확인
ros2 topic echo /velodyne_points

# 시각화 (RViz2)
rviz2
```
RViz2에서 `Fixed Frame`을 `velodyne`으로 설정하고, `PointCloud2` 디스플레이를 추가하여 `/velodyne_points` 토픽을 구독하면 3D 스캔 데이터를 볼 수 있습니다.

---

### 💡 팁: 네트워크 설정
벨로다인 라이다는 기본적으로 이더넷 통신을 사용합니다.
*   라이다의 기본 IP는 보통 `192.168.1.201`입니다.
*   사용자 PC의 유선 네트워크 IP를 `192.168.1.xxx` 대역(예: `192.168.1.100`)으로 설정해야 라이다와 통신이 가능합니다.

더 상세한 파라미터(IP 주소 변경, 포트 설정 등)는 각 패키지의 `config` 폴더 내 YAML 파일을 수정하거나 런칭 시 인자로 전달할 수 있습니다.

